build:
  docker:
    web: Dockerfile
    worker: Dockerfile

run:
  web:
    command:
      - uvicorn api.__main__:api --host 0.0.0.0 --reload
    image: web
  worker:
    command:
      - celery --app api.utils.tasks.celery_client worker --loglevel=info
    image: web